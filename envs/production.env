# Production environment configuration
BASE_URL=https://api.example.com
TIMEOUT=60s
PARALLEL_WORKERS=1
RETRY_COUNT=5

# API Keys (production - should come from secure env vars)
API_KEY=${PROD_API_KEY}
AUTH_TOKEN=${PROD_AUTH_TOKEN}

# Production-specific settings
TEST_USER_ID=789
TEST_EMAIL=prod-test@example.com

# Safety settings for production
READ_ONLY=true
SKIP_DESTRUCTIVE_TESTS=true