name: Create a codespace in a repository
description: |-
  Creates a codespace owned by the authenticated user in the specified repository.

  OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
env: null
vars: null
setup: null
tests:
- name: Create a codespace in a repository
  description: |-
    Creates a codespace owned by the authenticated user in the specified repository.

    OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.
  request:
    method: POST
    url: https://api.github.com/repos/{owner}/{repo}/codespaces
    headers: null
    params: null
    body: |-
      {
        "client_ip": "example",
        "devcontainer_path": "example",
        "display_name": "example",
        "geo": "example",
        "idle_timeout_minutes": 42,
        "location": "example",
        "machine": "example",
        "multi_repo_permissions_opt_out": true,
        "ref": "example",
        "retention_period_minutes": 42,
        "working_directory": "example"
      }
  expect:
    status: 201
    schema: null
    jsonpath: null
    headers: null
dataset: null
teardown: null
